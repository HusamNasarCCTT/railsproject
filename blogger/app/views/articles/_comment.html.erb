<div class="comment-div">

  <h4>Comment by <%= comment.author_name%></h4>
  <p class="comment"><%= comment.body%></p>
  <!-- distance_of_time_in_words is a rails helper method which takes two time stamps as parameters
        and displays the time between them in words as a string-->
  <p>Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at)%> later</p>
</div>
